# Rust build artifacts
target/
**/*.rs.bk

# Internal only, never publish
internal/
**/deploy/keys/
**/trust.toml

# Secrets / keys / env
*.key
!*.pub
*.env
!*.env.example
*.secret

# Runtime data / state
*.db
*.db-wal
*.db-shm
**/blobs/
node.key
registry.db*
*-data/
*-stage/

# Local editor / agent settings
.claude/settings.local.json
.DS_Store

# Landing page (dist/ is committed)
landingpage/node_modules/
landingpage/.next/
